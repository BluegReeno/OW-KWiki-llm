# Digests

Dated summaries of offshoreWIND.biz newsletter issues, written automatically
by `pipeline/run.py` as new issues arrive. Empty until the pipeline is
running — see `pipeline/README.md` to set it up.
